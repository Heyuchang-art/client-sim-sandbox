# 证券客户行为沙盘 · 第五轮独立审核报告

审核对象：提交 `70bb26c`（工作区干净）
审核日期：2026-09-17

## 结论：本轮通过

未发现阻断级或重要级问题。第四轮的全部 P0/P1/P2/P3 已完成，且我逐条实测复核过。

## 第四轮意见完成度

| 第四轮建议 | 状态 | 复核依据 |
|---|---|---|
| P0 三处成因改为实测机制 | 已完成 | `sweep-strategies.ts:167-168`、`指标证据表.md:118`、docx 第 123 行均已写明「唤醒罚分随恐慌衰减、λ_wake 放大 8 倍、广播式唤醒基数更大因而减负更多、人工成本不随恐慌衰减」，并明确「恐慌并未饱和，因此不是饱和效应」。与我的实测一致（我对同参数的独立测量：广播式唤醒 40%→50% 衰减 38%，峰值恐慌 57.5%–76.5%，远离 1.0 上限） |
| P0 docx「任何权重下都必须成立」加限定 | 已完成 | 负向检查「任何权重下都必须成立」无命中；正向检查第 123 行含「跨权重成立」 |
| P1 前端补处理分支 | 已完成 | `lib/client/run-task.ts:156-159`（skill.reused）与 `:161-165`（skill.reuse_rejected）均写入 `state.notes`，中文描述，死路径消除 |
| P1 README 补 eval:strategies | 已完成 | `README.md:109` 有该命令行 |
| P1 sweep 脚本纳管 | 已完成 | `git ls-files scripts/sweep-strategies.ts` 有输出；`70bb26c` 含该文件 |
| P2 测试改为性质断言 | 已完成 | `lib/simulation.test.ts:380-382` 改为「违反非空 + 全部落在唤醒=8且6h + 不超过序列总数 10%」，不再断言精确条数 |
| P2 复用决策点注释写明前提 | 已完成 | `runner.ts:209` 注释写明「一旦放开 validatePlan 的工具序列约束，这里必须改为『复核通过后才采用编排』」 |
| P3 删除死赋值 | 已完成 | 复核分支内的 `reusedSkill = null` 已删（现存的 `:202` 是规划阶段 else 分支的合法赋值） |

## 一般级（不阻断通过，建议顺手修）

### 一般-1　三处成因文字有复制粘贴残留

- `scripts/sweep-strategies.ts:167-168`：`limits` 字符串把开头句重复了两遍（实测 `40%→50% 区间会出现一次深度回落` 出现 2 次、`该配置不属于默认口径` 出现 2 次）。
- `artifacts/指标证据表.md:118`：同样重复（开头句 2 次、结尾句 2 次），且同一条目内先用中文名「分群差异化沟通」，后接英文 id「segmented」，口径不统一。
- docx 第 123 行：开头句已去重（1 次），但结尾句「该配置不属于默认口径，已记录为模型迭代项」仍出现 2 次，且正文里出现英文 id「segmented」「broadcast」。

原因应是改写时把新文本追加在旧文本之后、未删除旧前缀与旧尾句。建议三处统一为单段、去掉英文 id。

### 一般-2　「峰值约 73%–79%」这个数字我复现不出来

三处均写「该场景峰值约 73%–79%，上限为 1.0」。我用扫描脚本 `extreme` 检查的同一组参数（6 小时 / 8 步 / 200 客户 / 种子 20260830 / 高波动回撤客群）实测：40% 档 broadcast 57.51%、segmented 62.45%、baseline 68.44%；50% 档 broadcast 70.37%、segmented 76.51%、baseline 83.79%。即实际跨度约 57.5%–83.8%，与 73%–79% 不符。

该数字只用于支撑「未饱和」这一判断，而判断本身成立（83.8% 仍低于 1.0），所以不影响结论；但既然写进了申报书，建议按实测值改写为「约 58%–84%」，或删掉具体区间只保留「明显低于 1.0」。

## 材料一致性复核

第三轮/第四轮指出的四类问题全部清空，负向检查全部无命中：「任何权重下都必须成立」「峰值位置后移」「共 67 项」「83.2 毫秒」「恐慌接近饱和」。正向检查全部命中：第 123 行含跨权重限定与新成因、第 54 行为 69 项、第 84/270 行为 71.5 毫秒、第 151 行为「峰值相应抬高」。docx 与指标表的耗时、测试项数、时长敏感性三个口径一致。

> 本轮结论基于提交 `70bb26c` 与本地规则模式实测。仅在 `.tmp/` 写入过分析脚本并已删除，未改动被审核项目的任何文件。
